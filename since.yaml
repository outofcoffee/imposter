requireBranch: main

ignore:
  - "prep for release"
  - "next development version"
  - "/examples/junit-sample"

before:
  - command: bash
    args: ["scripts/prerelease.sh"]

after:
  - command: bash
    args: ["scripts/postrelease.sh"]
